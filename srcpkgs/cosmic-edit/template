# Template file for 'cosmic-edit'
pkgname=cosmic-edit
version=1.0.0.beta.7
revision=1
build_options="sccache"
_tag=epoch-${version/.beta/-beta}
build_style=cargo
hostmakedepends="pkg-config just"
makedepends="libxkbcommon-devel oniguruma-devel libzstd-devel libglib-devel $(vopt_if sccache "rust-sccache")"
depends="libxkbcommon libzstd oniguruma"
short_desc="TODO: cosmic-edit short_desc"
maintainer="Bella Wagner <belladev109@proton.me>"
license="GPL-3.0-only"
homepage="https://github.com/pop-os/cosmic-edit"
distfiles="https://github.com/pop-os/cosmic-edit/archive/refs/tags/${_tag}.tar.gz"
checksum=32ea4bfdd2610ebd9e234dd7be7dc3b33bcdfbc00b5484d4b0ec4484af4dd192

#export VERGEN_GIT_COMMIT_DATE="${version}_${revision}"
#export VERGEN_GIT_SHA="${version}_${revision}"

do_build() {
	$(vopt_if sccache "export RUSTC_WRAPPER=/usr/bin/sccache")
	just build-release
}

do_install() {
	just --set rootdir ${DESTDIR} install
}
